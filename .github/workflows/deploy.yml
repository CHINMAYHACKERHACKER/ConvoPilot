name:Node Deployment CI/CD

on:
  push:
    branches:[ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest

steps:
  - name: Install dependencies
    run: npm install

  - name: Run the app
    run: npm run dev